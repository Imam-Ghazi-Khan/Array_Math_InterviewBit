 public int[] getRow(int A) {
        int ans[] = new int[A+1];
        ans[0] = 1;
        for(int i = 1,j = A; i <= A; i++,j--)
        {
            int c = ((ans[i-1]*j/i));
            ans[i]=c;
        }
        return ans;
    }
